import 'package:flutter/material.dart';

class LeaveScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('This is a Leave Screen', style: TextStyle(fontSize: 16)),
    );
  }
}
