import 'package:flutter/material.dart';

class SwapsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('This is a Swaps Screen', style: TextStyle(fontSize: 16)),
    );
  }
}
