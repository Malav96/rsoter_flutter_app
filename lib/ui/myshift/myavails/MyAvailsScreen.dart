import 'package:flutter/material.dart';

class MyAvailsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('This is a My Avails Screen', style: TextStyle(fontSize: 16)),
    );
  }
}
