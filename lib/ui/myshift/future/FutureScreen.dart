import 'package:flutter/material.dart';

class FutureScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('This is a Future Screen', style: TextStyle(fontSize: 16)),
    );
  }
}
