import 'package:flutter/material.dart';

class TodayScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('This is a Today Screen', style: TextStyle(fontSize: 16)),
    );
  }
}
