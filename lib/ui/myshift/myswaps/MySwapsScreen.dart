import 'package:flutter/material.dart';

class MySwapsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('This is a MySwaps Screen', style: TextStyle(fontSize: 16)),
    );
  }
}
